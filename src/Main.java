import javafx.application.Application;

/**
 * Program entry point — launches the JavaFX application.
 */
public class Main {

    public static void main(String[] args) {
        Application.launch(AppointmentSchedulerApp.class, args);
    }
}
